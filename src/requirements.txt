fastapi==0.115.6
uvicorn[standard]==0.32.1
SQLAlchemy==2.0.36
alembic==1.14.0
# SQLAlchemy ORM layer (pydantic >= 2.12.5 is required by google-genai)
pydantic>=2.12.5,<3
pydantic-settings==2.6.1
python-dotenv==1.0.1
python-multipart==0.0.19
email-validator==2.2.0

# Auth (Phase 1 — JWT pattern per SDD §3)
# NOTE: bcrypt is pinned to 4.0.1 because passlib 1.7.4 is not compatible
# with bcrypt >= 4.1 (the __about__ module was removed).
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0

# PDF handling (Phase 2 — page count only; real extraction arrives in Phase 3)
PyMuPDF==1.25.2

# PDF extraction (Phase 3 — native text/table/image extraction)
# NOTE: camelot-py 2.x is a major rewrite but keeps the API surface this
# project uses (read_pdf/pages/flavor, table.df, table._bbox).  Ghostscript
# (gswin64c on PATH) is still required for lattice mode.
camelot-py[cv]==2.0.0
pytesseract==0.3.13
pandas==2.2.3
tabulate==0.9.0

# LLM SDK + tokenization (Phases 5, 7, 8)
# NOTE: the code imports ``google.genai`` (the new google-genai SDK), which
# requires pydantic >= 2.12.5 — do not downgrade pydantic below that.
google-genai==2.24.0
tiktoken==0.9.0

# Embedding model (Phase 6 — sentence-transformers for local embeddings)
sentence-transformers==3.4.1

# Vector store (Phase 6 — ChromaDB for persistent vector index)
chromadb==1.0.6

# Text splitting (Phase 6 — LangChain text splitter for chunking)
langchain-text-splitters==0.3.6

# Testing (informal in every phase, formal suite in Phase 13)
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
