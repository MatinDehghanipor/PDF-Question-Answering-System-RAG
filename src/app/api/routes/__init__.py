"""FastAPI route modules for the PDF QA system.

Each module exposes an ``APIRouter`` that is mounted in ``app/main.py``.
Phase 0 stubs return hardcoded example payloads marked with ``"_stub": true``
so the full API surface is visible in Swagger before real logic exists.
"""