fastapi==0.115.6
uvicorn[standard]==0.32.1
SQLAlchemy==2.0.36
alembic==1.14.0
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1
python-multipart==0.0.19
email-validator==2.2.0

# Auth (Phase 1 — JWT pattern per SDD §3)
# NOTE: bcrypt is pinned to 4.0.1 because passlib 1.7.4 is not compatible
# with bcrypt >= 4.1 (the __about__ module was removed).
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0

# PDF handling (Phase 2 — page count only; real extraction arrives in Phase 3)
PyMuPDF==1.25.2

# PDF extraction (Phase 3 — native text/table/image extraction)
camelot-py[cv]==0.11.0
pytesseract==0.3.13
pandas==2.2.3
tabulate==0.9.0

# Testing (informal in every phase, formal suite in Phase 13)
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
