"""Test suite for the PDF QA system backend.

Pytest-based tests.  Phase 0 ships a smoke test that verifies the skeleton
imports cleanly and exposes every router; deeper per-phase tests are added
alongside each implementation phase (formal suite in Phase 13).
"""